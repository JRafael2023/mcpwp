# Core MCP y utilidades
mcp>=1.0.0
pydantic>=2.0.0
python-dotenv>=0.19.0

# Cliente HTTP
httpx>=0.25.0
requests>=2.31.0

# Servidor HTTP (FastAPI)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# IA - Ollama (API gratuita)
# No requiere paquete adicional, solo requests 